# 提交报告（2026-03-15）

## 本次目标

围绕“让 AI 快速理解并稳定维护项目”完成文档体系建设、过期文档清理与索引重构，并对核心内容进行二次复核。

## 主要交付

### 1. 新增 AI 导航文档

- `AI_QUICKSTART.md`  
  10 分钟上手路径：项目目标、启动方式、核心链路、排错入口。
- `SYSTEM_MAP.md`  
  全站系统边界、模块关系、关键调用流、故障定位路径。
- `API_CONTRACTS.md`  
  前后端接口契约：请求/响应字段、latest/previous 版本逻辑、错误格式、兼容约束。
- `DATA_DICTIONARY.md`  
  武器/弹药/护甲/头盔/配件字段字典与回退规则。
- `AI_TASK_PLAYBOOK.md`  
  AI 执行任务标准流程：改动前检查、模块改动规则、回归模板、交付模板。

### 2. 页面级文档归档

新增页面文档目录与逐页说明：
- `docs/pages/README.md`
- `docs/pages/APP_SHELL_PAGE.md`
- `docs/pages/DATA_QUERY_PAGE.md`
- `docs/pages/SIMULATOR_PAGE.md`
- `docs/pages/DATA_LIBRARY_PAGE.md`

每份页面文档包含：页面职责、状态结构、主流程、依赖关系、异常处理、测试清单。

### 3. 文档索引重构

- 重写 `DOCS_INDEX.md`
- 建立 AI 优先阅读路径：Quickstart -> Playbook -> System Map -> API Contracts -> Data Dictionary
- 移除已过期入口，补齐新增文档入口

### 4. 过期文档清理

删除旧阶段报告：
- `提交报告_20260306.md`
- 旧版 `提交报告_20260315.md`（已由本报告替代）

## 复核结果（代码对齐）

已按真实代码核对以下关键事实：

- 后端端口当前为硬编码 `3001`（`backend/index.js`），文档已修正一致。
- API 路由与字段一致：`/available-guns`、`/gun-details`、`/combinations`、`/ttk-curve`。
- `DataQuery` 的 `latest/previous` 版本取数逻辑与文档一致。
- `useGameData` 的远程优先、本地回退、重试与缓存行为已对齐文档。
- `simulationUtils` 的穿透/甲伤/部位倍率与 `PD12双头弹` 特殊分支已纳入说明。

## 影响范围

- 主要影响：项目文档体系、AI 接手效率、维护规范清晰度。
- 无破坏性数据库结构变更。
- 前后端业务接口契约未在本次文档整理中变更。

## 后续建议

1. 每次改 API 字段时，同步更新 `API_CONTRACTS.md`。  
2. 每次改数据结构时，同步更新 `DATA_DICTIONARY.md`。  
3. 每次改页面行为时，同步更新 `docs/pages/*` 与 `WEBSITE_LOGIC_ARCHIVE.md`。  

# 更新报告（2026-03-21）

## 一、前端页面与交互调整

### 1. 页面入口与命名
- 原「Monte Carlo TTK」入口调整为「概率TTK折线图」并置顶。
- 原「概率TTK折线图」入口更名为「预制TTK折线图」。

### 2. Monte Carlo 页面体验优化
- 对比图保留折线图模式，移除柱状图模式。
- 将「TTK / BTK / 扳机延迟 / 枪口初速」统一为同一行按钮交互。
- 折线图与柱状图（历史逻辑）tooltip 样式统一为半透明+圆角，并修复“距离”文案乱码。
- 空态文案（请先运行方案）样式简化：去虚线框、去浅蓝背景、字号提升。

### 3. 方案与配置面板
- 方案列表卡片样式优化（图标、层次、颜色与信息密度调整）。
- 配置弹窗布局优化（分区、间距、控件一致性、移动端适配）。
- 必填项未选时高亮提示，选择后自动消除。

---

## 二、自定义枪械 / 子弹功能完善

### 1. 自定义创建与编辑流程
- 支持在页面内新增自定义枪械与自定义子弹。
- 新增/修改时支持模板带入并继续编辑。
- 修复“修改入口重复点击导致连环命名（11/21/211）”问题。

### 2. 自定义对象配对与实例化
- 修复模板切换时枪/弹图片继承状态互相污染问题。
- 自定义条目命名去重与自动后缀（1/2/3…）保持一致。

### 3. 显示与列表策略
- 自定义子弹隐藏参数摘要，仅保留名称显示。
- 自定义子弹名称支持按穿透值着色。

---

## 三、护甲/头盔选择策略调整（两页同步）

生效页面：
- 概率TTK折线图（原 Monte Carlo）
- 伤害模拟器

调整内容：
- 头盔与护甲采用“同类裁剪”策略，减少仅耐久差异造成的重复项。
- 同类中仅保留最高耐久代表项（并沿用代表图）。
- 命名统一（头盔：X级头盔；护甲：X级甲/护腹/护肩差异化展示）。

---

## 四、稳定性与构建

- 多轮前端构建验证通过：`npm run build -> Compiled successfully`。
- 清理临时 `.acpx_*` 任务文件（本次提交前已清理新增临时文件）。

---

## 五、后续建议

1. 上线前在目标环境验证一次：
   - 枪口配件可见性
   - 自定义枪/弹编辑与命名
   - 两页面护甲裁剪结果

2. 服务器侧部署建议保留回滚版本：
   - 部署新包前备份旧 `build` 目录
   - 出现异常可快速回退

---

## 六、本轮补充（页面默认入口与文案统一）

### 1. 默认首页调整
- 应用默认进入「模拟期望击杀时间」页面（原概率TTK页），减少首次进入的点击路径。

### 2. 顶部横幅文案更新
- 主标题从 `DeltaForce TTK` 调整为：`三角洲行动 枪械击杀用时查询`。
- 标题旁新增数据标识：`当前数据：S9季中更新`。

### 3. 侧边栏名称统一
- `概率TTK折线图` 更名为 `模拟期望击杀时间`。
- `预制TTK折线图` 更名为 `查询期望击杀时间`。

### 4. 样式与适配
- 为顶部标题新增组合容器样式与数据标识样式。
- 在平板/移动端下同步调小数据标识字号，保持布局稳定。

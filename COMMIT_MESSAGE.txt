feat: 实现命中率控制和护甲耐久自动刷新功能

## 核心功能

### 1. 命中率系统集成
- 将命中率控制从对比列表移至配件选择弹窗
- 使用 UniversalSlider 组件保持 UI 风格统一
- 实现 localStorage 记忆功能，保存用户最后设置的命中率
- 命中率范围：30%-100%，步进 5%
- 添加至对比按钮显示当前命中率百分比

### 2. 护甲耐久自动刷新
- 实现护甲耐久值变化时自动重新查询 BTK 数据
- 使用 useRef + useCallback 解决异步闭包陷阱
- 使用防重入锁避免重复刷新
- 自动区分耐久调整 vs 护甲切换场景
- 保留武器配置（枪械、子弹、配件），仅更新 BTK 数据

### 3. 对比列表简化
- 恢复简洁的列表样式
- 移除所有命中率显示和控制 UI
- 优化删除按钮位置（绝对定位）
- 保留：颜色指示、武器名、子弹稀有度、配件数量

## 技术实现

### 数据流优化
- 引入 `comparisonLinesRef` 同步最新对比线数据
- 引入 `prevArmorConfigRef` 追踪护甲配置变化
- 引入 `isRefreshingRef` 防止重复刷新

### API 数据结构统一
- 修复刷新功能的数据结构不匹配问题
- 统一使用 `allDataPoints` 格式：
  ```js
  {
    availableBullets: ['BP', 'SP6'],
    allDataPoints: [
      {bullet_name: 'BP', distance: 10, btk_data: '[...]'},
      ...
    ]
  }
  ```

### 命中率计算逻辑
- 在 `dataProcessor.js` 中集成命中率调整
- 使用公式：`adjustedEbtk = baseEbtk / hitRate`
- 例：5发击杀，80%命中率 → 6.25发期望

### 模拟数据保护
- 检测模拟数据存在时显示警告
- 切换护甲/头盔时弹出确认框
- 保留模拟数据但提示可能不准确

## 代码质量

### 调试日志清理
- 移除所有非错误的 console.log
- 保留关键错误日志用于问题追踪
- 清理 ComparisonList、ModificationModal、DataQuery、dataProcessor 中的调试代码

### 布局优化
- ModificationModal 改用 block 布局 + margin-bottom
- 修复弹窗滚动问题
- 修复移动端元素压缩问题

### 样式改进
- 统一命中率滑块样式（添加 % 后缀）
- 优化配件提示工具 tooltip 样式
- 改进响应式设计

## 新增文件

- `frontend/src/assets/data/gameConstants.js`
  - 游戏常量配置（玩家血量、命中概率、PD12 钝伤系数等）
  - 为 Phase 3 前端模拟功能预留接口

## 改动文件

### Frontend
- `src/components/data_query/ComparisonList.jsx` - 简化列表显示
- `src/components/data_query/ComparisonList.css` - 优化样式
- `src/components/public/ModificationModal.jsx` - 集成命中率控制
- `src/components/public/ModificationModal.css` - 修复布局和样式
- `src/pages/DataQuery.js` - 实现自动刷新逻辑
- `src/utils/dataProcessor.js` - 集成命中率计算

## 功能验证

✅ 命中率记忆功能正常
✅ 命中率影响 TTK 计算正确
✅ 护甲耐久自动刷新工作正常
✅ 对比列表样式简洁
✅ 弹窗滚动无问题
✅ 模拟数据警告提示正常
✅ API 数据结构统一

## Breaking Changes

无破坏性更改。所有改动向后兼容。

## Next Steps

- [ ] 实现切换护甲时的自动刷新
- [ ] 实现数据完整性检查对话框
- [ ] Phase 3: 前端模拟计算功能
- [ ] Phase 3: PD12 特殊处理逻辑
feat(ttk): 支持查看“上版本”数据并切换展示

变更说明：
- 后端 `/api/ttk/gun-details`：基于 create_at/created_at 的历史记录，按 bullet_name 聚合并计算“最新版本/上版本”（仅当同一子弹数据内容发生变化且时间不同才视为新版本），返回 `previousAllDataPoints` 等字段供前端切换。
- 前端改装弹窗：新增“其他选项”区块（仅在存在上版本数据时显示），提供“显示上版本数据”勾选；切换/变体变更时仅在弹药不可用才清空选择。
- 对比线：保存 dataVersion（latest/previous），刷新/预检查时按该版本取后端对应数据。
- 数据修复：调整 `frontend/src/assets/data/ammos.js` 中 BT +P 稀有度配置。

影响范围：
- backend/routes/ttk.js
- frontend/src/components/public/ModificationModal.jsx
- frontend/src/pages/DataQuery.js
- frontend/src/assets/data/ammos.js
